#pragma once

#include "Core.h"

NAMESPACE_BEGAN

// this class is an interphase so for diferent platforms we are going to define diferent ways to read input

//class SHADOW_API Input
//{
//public:
//	static bool IsKeyPressed(int keycode) { return instance->IsKeyPressedImplementation(keycode); }
//protected:
//	virtual bool IsKeyPressedImplementation(int keycode) = 0;
//private:
//	static Input* instance;
//};


NAMESPACE_END